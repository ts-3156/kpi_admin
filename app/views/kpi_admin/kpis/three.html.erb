<%= render partial: 'selector' %>

<div><%= link_to('KPIs', root_path, class: 'btn btn-default btn-lg') %></div>

<div class="row">
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis sign_in_uu"></div>
  </div>
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis sign_in_num"></div>
  </div>

  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis welcome_to_login_uu"></div>
  </div>
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis welcome_to_login_ctr_cvr"></div>
  </div>

  <div class="col-xs-12"><h3>Per device_type</h3></div>

  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis sign_in_uu_per_device_type"></div>
  </div>
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis sign_in_num_per_device_type"></div>
  </div>

  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis welcome_to_login_uu_per_device_type"></div>
  </div>
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis welcome_to_login_ctr_cvr_per_device_type"></div>
  </div>

  <div class="col-xs-12"><h3>Per channel</h3></div>

  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis sign_in_uu_per_channel"></div>
  </div>
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis sign_in_num_per_channel"></div>
  </div>

  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis welcome_to_login_uu_per_channel"></div>
  </div>
  <div class="col-xs-12 col-sm-6">
    <%= image_tag asset_path('kpi_admin/ajax-loader.gif'), class: 'loading' %>
    <div class="kpis welcome_to_login_ctr_cvr_per_channel"></div>
  </div>
</div>

<div><%= link_to('KPIs', root_path, class: 'btn btn-default btn-lg') %></div>

<script>
  function reload() {
    $('.loading').show();
    $('.kpis').empty();
    $.each(charts, function (_, chart) {
      chart.destroy();
    });
    charts = {};

    var url = "<%= one_path %>";
    var types = [
      'sign_in_uu',
      'sign_in_num',
      'welcome_to_login_uu',
      'welcome_to_login_ctr_cvr',
      'sign_in_uu_per_device_type',
      'sign_in_num_per_device_type',
//      'welcome_to_login_uu_per_device_type',
//      'welcome_to_login_ctr_cvr_per_device_type',
      'sign_in_uu_per_channel',
      'sign_in_num_per_channel'
//      'welcome_to_login_uu_per_channel',
//      'welcome_to_login_ctr_cvr_per_channel'
    ];
    fetch(url, types[0])
        .then(function(){return fetch(url, types[1])})
        .then(function(){return fetch(url, types[2])})
        .then(function(){return fetch(url, types[3])})
        .then(function(){return fetch(url, types[4])})
        .then(function(){return fetch(url, types[5])})
        .then(function(){return fetch(url, types[6])})
        .then(function(){return fetch(url, types[7])});
  }

  $(function () {
    after_load();
    reload();
  });
</script>
